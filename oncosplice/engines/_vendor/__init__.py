"""Vendored third-party model code.

Each sub-package here is an upstream model definition copied verbatim so that
oncosplice can run the model without a separate source checkout. Provenance and
license are recorded in each sub-package's ``__init__`` and its ``LICENSE`` file.
Only license-compatible code (MIT) is vendored here; see
:mod:`oncosplice.engines._pangolin_arch` for the GPL-incompatible Pangolin case,
which is independently re-implemented rather than copied.
"""
